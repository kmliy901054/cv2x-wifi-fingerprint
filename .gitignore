# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# ROS 2 build artifacts
build/
install/
log/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
*~

# Intermediate / temp
*.tmp
*.bak

# Large binary intermediates (we keep .pgm but exclude raw point cloud dumps if any)
*.pbstream

# rqt cache
.rqt_pose_config

# Lab 3 — model checkpoints. Ignore all 90 (239 MB), but keep the 5-seed
# winning model (Cascade 2-level, ~18 MB) so the best result is reproducible.
code/lab3/outputs/checkpoints/*
!code/lab3/outputs/checkpoints/A_random__Cascade_s4[2-6].pt

# Claude Code working files (local notes / settings, not project deliverables)
.claude/
memory/
